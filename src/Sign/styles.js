import styled from 'styled-components';

export const ImageElement = styled.img`
    width: 30px
`;

export const EmptyElement = styled.span`
    width:30px;
    height:30px;
    display:inline-block;
    background:black;
`;

export const SignElement = styled.span`
    display:inline-block
`;

